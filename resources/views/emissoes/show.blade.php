@extends('layouts.app') @section('title','Visualizar emissão') @section('content') @if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<h1 class="page-title mb-4">Visualizar emissão</h1><div class="card content-card"><div class="card-body p-4"><div class="row g-3">@foreach([['ID',$certificado->id],['Emissão',$certificado->nome],['Template',$certificado->template?->nome],['Participantes',$certificado->participantes_count],['Data',$certificado->data_emissao?->format('d/m/Y')],['Status',$certificado->ativo?'Ativa':'Inativa']] as [$l,$v])<div class="col-md-6"><div class="form-label fw-semibold">{{ $l }}</div><div class="form-control bg-body-tertiary">{{ filled($v)?$v:'—' }}</div></div>@endforeach</div><div class="d-flex justify-content-end gap-2 mt-4">@if(in_array('emissoes.participantes',(array)session('gi_context.permissoes',[]),true))<a href="{{ route('emissoes.participantes',$certificado) }}" class="btn btn-success"><i class="bi bi-people me-1"></i>Participantes e PDFs</a>@endif @if(in_array('emissoes.editar',(array)session('gi_context.permissoes',[]),true))<a href="{{ route('emissoes.edit',$certificado) }}" class="btn btn-primary">Editar</a>@endif<a href="{{ route('emissoes.index') }}" class="btn btn-outline-secondary">Voltar</a></div></div></div>@endsection
