@extends('layouts.app') @section('title','Participantes da emissão') @push('styles')<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet">@endpush @section('content')
<div class="d-flex justify-content-between"><h1 class="page-title">Participantes da emissão #{{ $certificado->id }}</h1>@if(in_array('emissoes.gerar_pdfs',$permissions,true)&&$items->isNotEmpty())<form method="POST" action="{{ route('emissoes.generate',$certificado) }}">@csrf<button class="btn btn-danger"><i class="bi bi-file-earmark-pdf me-1"></i>Gerar certificados</button></form>@endif</div>@if(session('status'))<div class="alert alert-success mt-3">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert alert-danger mt-3">{{ $errors->first() }}</div>@endif
@if(in_array('emissoes.inserir_participantes',$permissions,true))<form method="POST" action="{{ route('emissoes.participantes.store',$certificado) }}" class="card content-card my-4"><div class="card-body">@csrf<label class="form-label">Adicionar participantes</label><select id="participantes" name="participantes[]" class="form-select" multiple required></select><div class="text-end mt-3"><button class="btn btn-primary">Adicionar selecionados</button></div></div></form>@endif
<div class="card content-card"><div class="card-body p-0"><table class="table table-hover mb-0"><thead><tr><th>ID</th><th>Nome</th><th>E-mail</th><th>Geração</th><th>Ações</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->participante_id }}</td><td>{{ $item->participante?->nome??'—' }}</td><td>{{ $item->participante?->email??'—' }}</td><td>@if($item->arquivoExists() && in_array('emissoes.visualizar',$permissions,true))<a target="_blank" rel="noopener noreferrer" href="{{ route('emissoes.participantes.pdf',[$certificado,$item]) }}" class="btn btn-sm btn-outline-danger listagem-acao" title="Abrir PDF" aria-label="Abrir PDF"><i class="bi bi-file-earmark-pdf-fill"></i></a>@elseif(in_array('emissoes.gerar_pdfs',$permissions,true))<form method="POST" action="{{ route('emissoes.participantes.generate',[$certificado,$item]) }}" class="d-inline">@csrf<button class="btn btn-sm btn-link p-0">Gerar PDF</button></form>@elseif($item->erro_geracao)<span class="text-danger" title="{{ $item->erro_geracao }}">Erro</span>@else<span class="text-muted">Pendente</span>@endif</td><td>@if(in_array('emissoes.excluir_participantes',$permissions,true))<form method="POST" action="{{ route('emissoes.participantes.destroy',[$certificado,$item]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">Nenhum participante adicionado.</td></tr>@endforelse</tbody></table></div></div><div class="text-end mt-3"><a href="{{ route('emissoes.show',$certificado) }}" class="btn btn-outline-secondary">Voltar</a></div>@endsection
@push('scripts')<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script><script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script><script>$('#participantes').select2({theme:'bootstrap-5',width:'100%',placeholder:'Pesquise por nome ou e-mail',ajax:{url:@json(route('emissoes.participantes.opcoes',$certificado,false)),dataType:'json',data:p=>({q:p.term||'',page:p.page||1}),processResults:r=>r}})</script>@endpush
