@extends('layouts.app')
@section('title','Participantes do certificado')
@push('styles')<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet">@endpush
@section('content')
<h1 class="page-title">Participantes do certificado #{{ $certificado->id }}</h1>@if(session('status'))<div class="alert alert-success mt-3">{{ session('status') }}</div>@endif @if(in_array('novos_certificados.inserir_participantes',$permissions,true))<form method="POST" action="{{ route('certificadosnovos.participantes.store',$certificado) }}" class="card content-card my-4"><div class="card-body">@csrf<label class="form-label">Adicionar participantes</label><select id="participantes" name="participantes[]" class="form-select" multiple required></select><div class="text-end mt-3"><button class="btn btn-primary">Adicionar selecionados</button></div></div></form>@endif<div class="card content-card"><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>ID</th><th>Nome</th><th>E-mail</th><th>Ações</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->participante_id }}</td><td>{{ $item->participante?->nome??'—' }}</td><td>{{ $item->participante?->email??'—' }}</td><td>@if(in_array('novos_certificados.excluir_participantes',$permissions,true))<form method="POST" action="{{ route('certificadosnovos.participantes.destroy',[$certificado,$item]) }}" onsubmit="return confirm('Remover este participante?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@endif</td></tr>@empty<tr><td colspan="4" class="text-center text-muted">Nenhum participante adicionado.</td></tr>@endforelse</tbody></table></div></div></div><div class="text-end mt-3"><a href="{{ route('certificadosnovos.show',$certificado) }}" class="btn btn-outline-secondary">Voltar</a></div>
@endsection
@push('scripts')<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script><script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script><script>document.addEventListener('DOMContentLoaded',()=>{$('#participantes').select2({theme:'bootstrap-5',width:'100%',placeholder:'Pesquise por nome ou e-mail',ajax:{url:@json(route('certificadosnovos.participantes.opcoes',$certificado, false)),dataType:'json',delay:250,data:p=>({q:p.term||'',page:p.page||1}),processResults:r=>r}})});</script>@endpush
