@extends('layouts.app')
@section('title','Visualizar certificado novo')
@section('content')
@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<h1 class="page-title mb-4">Visualizar certificado novo</h1><div class="card content-card"><div class="card-body p-4"><div class="row g-3">@foreach([['ID',$certificado->id],['Certificado antigo',$certificado->certificadoAntigo ? ('#'.$certificado->certificadoAntigo->id.' - '.$certificado->certificadoAntigo->nome.' - '.$certificado->certificadoAntigo->atividade?->nome) : null],['Template',$certificado->template?->nome],['Participantes',$certificado->participantes_count],['Status',$certificado->ativo?'Ativo':'Inativo']] as [$label,$value])<div class="col-12 col-md-6"><div class="form-label fw-semibold">{{ $label }}</div><div class="form-control bg-body-tertiary h-auto">{{ filled($value)?$value:'—' }}</div></div>@endforeach</div><div class="d-flex flex-wrap justify-content-end gap-2 mt-4">@if(in_array('novos_certificados.participantes',(array)session('gi_context.permissoes',[]),true))<a href="{{ route('certificadosnovos.participantes',$certificado) }}" class="btn btn-success"><i class="bi bi-people me-1"></i>Adicionar participantes</a>@endif @if(in_array('novos_certificados.editar',(array)session('gi_context.permissoes',[]),true))<a href="{{ route('certificadosnovos.edit',$certificado) }}" class="btn btn-primary">Editar</a>@endif<a href="{{ route('certificadosnovos.index') }}" class="btn btn-outline-secondary">Voltar</a></div></div></div>
@endsection
