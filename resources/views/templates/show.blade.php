@extends('layouts.app')
@section('title','Visualizar template')
@section('content')
@if(session('status'))<div class="alert alert-success alert-dismissible fade show">{{ session('status') }}<button class="btn-close" data-bs-dismiss="alert"></button></div>@endif<div class="mb-4"><h1 class="page-title">Visualizar template</h1><p class="page-description mb-0">Dados do modelo de certificado.</p></div><div class="card content-card"><div class="card-header"><h2 class="h5 fw-bold mb-0">Dados do template</h2></div><div class="card-body p-4"><div class="row g-3">
@foreach([['ID',$template->id],['Nome',$template->nome],['Certificado A1',$template->certificadoA1 ? ('#'.$template->certificadoA1->id.' · '.$template->certificadoA1->nome) : null],['Status',$template->ativo?'Ativo':'Inativo'],['Página',$template->pagina],['Layout',$template->layout_pagina],['Largura',$template->largura],['Altura',$template->altura],['Criado em',$template->crido_em?->format('d/m/Y H:i')],['Alterado em',$template->alterado_em?->format('d/m/Y H:i')]] as [$label,$value])<div class="col-12 col-md-4"><div class="form-label fw-semibold">{{ $label }}</div><div class="form-control bg-body-tertiary h-auto text-break">{{ filled($value)?$value:'—' }}</div></div>@endforeach
@if($template->backgroundExists())<div class="col-12"><div class="form-label fw-semibold">Imagem de fundo</div><img src="{{ $template->backgroundUrl() }}" class="img-fluid border rounded" style="max-height:32rem" alt="Imagem de fundo"></div>@elseif($template->fundo)<div class="col-12"><div class="alert alert-warning">O arquivo <strong>{{ $template->fundo }}</strong> não foi encontrado.</div></div>@endif
</div><div class="d-flex justify-content-end gap-2 mt-4">@if(in_array('template.editar',(array)session('gi_context.permissoes',[]),true))<a href="{{ route('templates.edit',$template) }}" class="btn btn-primary"><i class="bi bi-pencil-fill me-1"></i>Editar</a>@endif<a href="{{ route('templates.index') }}" class="btn btn-outline-secondary">Voltar</a></div></div></div>
@endsection
