@extends('layouts.app') @section('title',$imagem->exists?'Editar imagem':'Nova imagem') @section('content')<h1 class="page-title mb-4">{{ $imagem->exists?'Editar imagem':'Nova imagem da biblioteca' }}</h1><form method="POST" enctype="multipart/form-data" action="{{ $imagem->exists?route('biblioteca_imagens.update',$imagem):route('biblioteca_imagens.store') }}">@csrf @if($imagem->exists)@method('PUT')@endif<div class="card content-card"><div class="card-body p-4">@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<div class="row g-3"><div class="col-md-5"><label class="form-label">Nome *</label><input name="nome" maxlength="120" required class="form-control" value="{{ old('nome',$imagem->nome) }}"></div><div class="col-md-4"><label class="form-label">Categoria *</label><select name="categoria" class="form-select" required>@foreach($categories as $key=>$label)<option value="{{ $key }}" @selected(old('categoria',$imagem->categoria)===$key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Status *</label><select name="ativo" class="form-select"><option value="1" @selected(old('ativo',$imagem->exists?(int)$imagem->ativo:1)==1)>Ativa</option><option value="0" @selected(old('ativo',$imagem->exists?(int)$imagem->ativo:1)==0)>Inativa</option></select></div><div class="col-12"><label class="form-label">Arquivo {{ $imagem->exists?'':'*' }}</label><input type="file" name="imagem" class="form-control" accept="image/png,image/jpeg,image/webp" @required(!$imagem->exists)>@if($imagem->url())<img src="{{ $imagem->url() }}" class="mt-3 border rounded" style="max-width:280px;max-height:160px" alt="">@endif</div></div><div class="text-end mt-4"><a href="{{ route('biblioteca_imagens.index') }}" class="btn btn-outline-secondary">Cancelar</a><button class="btn btn-primary">Salvar</button></div></div></div></form>@endsection
